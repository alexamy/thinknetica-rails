<%= form_with model: [:admin, @test, @question], local: true do |f| %>
  <div class="form-group">
    <%= f.label :body %>
    <%= f.text_area :body, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.submit t('h.submit'), class: 'btn btn-primary' %>
  </div>
<% end %>
