<h1><%= t('.header') %></h1>

<p>
  <%= link_to t('.add_new'), new_admin_test_path %>
</p>

<%= render 'shared/errors', resource: @test %>

<table class="table">
  <tr>
    <th class="sort-by-label pointer">
      <%= t('.label') %>
      <%= bootstrap_icon 'arrow-up-short', width: 25, height: 25, class: 'text-success hide' %>
      <%= bootstrap_icon 'arrow-down-short', width: 25, height: 25, class: 'text-success hide' %>
    </th>
    <th><%= t('.level') %></th>
    <th><%= t('.questions-count') %></th>
    <th><%= t('.completion-time') %></th>
    <th colspan="4"><%= t('.actions') %></th>
  </tr>

  <%= render @tests %>
</table>
