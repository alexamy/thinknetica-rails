<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <span class="navbar-text mr-auto">
    <%= link_to 'TestGuru', root_path, class: 'navbar-brand' %>
    <%= link_to admin_tests_path do %>
      <%= bootstrap_icon 'hammer', width: 25, height: 25 if admin? %>
    <% end %>
  </span>

  <span class="navbar-text mr-auto text-uppercase font-weight-bold">
    Панель управления TestGuru
  </span>

  <span class="navbar-text">
    <% if user_signed_in? %>
      Добро пожаловать, <b><%= current_user.email %></b>
      <%= link_to 'Выйти', destroy_user_session_path, method: :delete if user_signed_in? %>
    <% end %>
  </span>
</nav>
