<div class="navigation">
  <p><%= link_to 'TestGuru', root_path %></p>

  <span class="navigation-admin title">Панель управления TestGuru</span>

  <% if user_signed_in? %>
    <p class="navigation-admin user">Добро пожаловать, <b class="navigation-admin user-email"><%= current_user.email %></b></p>
  <% end %>

  <div class="actions">
    <%= link_to 'Выйти', destroy_user_session_path, method: :delete if user_signed_in? %>
  </div>
</div>
