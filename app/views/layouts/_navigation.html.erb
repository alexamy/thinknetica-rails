<div class="nav">
  <p><%= link_to 'TestGuru', root_path %></p>

  <% if user_signed_in? %>
    <p class="user">Добро пожаловать, <b><%= current_user.email %></b></p>
  <% end %>

  <div class="actions">
    <%= link_to 'Регистрация', new_user_registration_path %>
    <%= link_to 'Войти', new_user_session_path unless user_signed_in? %>
    <%= link_to 'Выйти', destroy_user_session_path, method: :delete if user_signed_in? %>
  </div>
</div>
